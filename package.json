{
  "name": "@tanaabased/prepare-release-action",
  "version": "1.2.1",
  "description": "An action that can automate release steps and sync them back to the releasing repo.",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "version-injector": "./dist/version-injector"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "contributors": [
    "Mike Pirog @pirog"
  ],
  "license": "MIT",
  "repository": "https://github.com/tanaabased/prepare-release-action",
  "bugs": "https://github.com/tanaabased/prepare-release-action/issues",
  "homepage": "https://github.com/tanaabased/prepare-release-action",
  "keywords": [
    "Github Actions"
  ],
  "engines": {
    "bun": ">=1.3.0"
  },
  "packageManager": "bun@1.3.4",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "@actions/github": "6",
    "jsonfile": "^6.1.0",
    "lodash.set": "^4.3.2",
    "semver": "^7.7.4"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "ansis": "^4.2.0",
    "c8": "^11.0.0",
    "debug": "^4.4.3",
    "eslint": "^10.1.0",
    "eslint-config-prettier": "^10.1.1",
    "globals": "^17.4.0",
    "mocha": "^11.7.5",
    "prettier": "^3.5.3",
    "yargs-parser": "21.1.1"
  },
  "scripts": {
    "test": "c8 --all --include \"utils/*.js\" --include \"bin/*.js\" --reporter=text --check-coverage --per-file --lines 80 mocha \"test/**/*.spec.js\"",
    "lint": "eslint . --format json",
    "format:check": "prettier . --check --ignore-unknown",
    "format:write": "prettier . --write --ignore-unknown",
    "build": "bun ./scripts/build.js",
    "prepare": "bun run build"
  }
}
